# Archived Redundant Results

Archived on 2026-06-03.

These files were moved out of the top-level `results/` directory to keep active experiment outputs readable. They were not permanently deleted.

Contents include:

- empty debug directories;
- incomplete runs without `eval_results.json` or generated outputs;
- custom beam / staged prefill-decode experiments that were judged not directly comparable with the current HF `generate` baseline;
- an old calibration-only M2 artifact/log without full evaluation.

If these are no longer needed, this whole archive folder can be removed manually.
